[project]
name = "fil-cijoe"
version = "0.3.4"
description = "CIJOE environment for fil"
requires-python = ">=3.10"

dependencies = [
    "cijoe==0.9.60",
    "withcache",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
